node_modules/
dist/
coverage/
*.log
.DS_Store

# ── Secrets & Keys (NEVER commit) ───────────
keys/
secrets/
*.pem
*.key
/run/secrets/
!package-lock.json

# ── Environment files ───────────────────────
.env
.env.*
.env*
*.env
!.env.example

# ── Deprecated config (replaced by docker-compose environment:) ──
.env.config

# ── Reports (generated by attack simulations) ──
reports/

# ── K8s manifests (separate repo) ──
k8s/
